# PresenceKit

A Swift package for local, low-overhead room-presence sensing on macOS.

This repository is being initialized from the RoomSense prototype as a reusable, concurrency-safe library. Motion detection is the baseline; Apple Vision recognition is optional and budgeted. Presence is evidence-based, not a guarantee of physical absence. The host application owns media playback and power policy.
